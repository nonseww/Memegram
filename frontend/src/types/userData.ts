export interface UserProfile {
  name: string;
  username: string;
  postsCount: number;
  followersCount: number;
  followingsCount: number;
  imageUrl: string;
  avatarUrl: string;
  aboutText: string;
}
